import * as crypto from '@cosmjs/crypto'
import * as sovrin from 'sovrin-did'
import * as ixohelper from './ixohelper'

async function init() {
    // sovrin.fromSeed(crypto.sha256(ixohelper.mnemonic_opera).slice(0, 32))
}